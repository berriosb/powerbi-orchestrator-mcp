"""Visualization / UX layer (Capa 5).

**Status:** v1.0.0 ships only the WCAG auditor + theme generator (which
live in ``src/powerbi_orchestrator_mcp/validation/`` and
``src/powerbi_orchestrator_mcp/tools/apply_theme_and_accessibility_rules.py``
respectively). The other modules listed in
[`specs/04-viz-ux.md`](../specs/04-viz-ux.md) — `VisualRegistry`,
`VisualSuggester`, `LayoutOptimizer`, `StorytellingScorer`,
`PerformanceBudget` — are **v2 work** (Sprints 9-11) per
[`docs/MVP-STATUS.md`](../docs/MVP-STATUS.md).

This package exists as a placeholder so that v2 imports resolve cleanly
and so that we can document the descoping decision here (rather than
silently).
"""
